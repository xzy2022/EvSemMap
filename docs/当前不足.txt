数据集中一帧雷达对应多张图像，每张图像都有一次语义预测，但是我这里每帧都只选择了第一张图像的预测。

一帧数据落在同一个体素单元的点云，没有被贝叶斯融合而是直接求平均，因为方法认为贝叶斯融合应该发生在不同时序。

可视化的结果2和结果3中，都只是全部数据的预测值、预测不确定性的评估，而地图建图时不确定性减小的实际作用没有体现。

03_Uncertainty_Calculation.ipynb实现了建图、帧间融合和可视化，作为最终的实验报告数据。