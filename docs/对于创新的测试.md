
`SemanticMap/src/SemanticMap/config/datasets/deploy_rellisv3_4_1-30.yaml`修改超参数`ell`和`sf2`。

建图
```sh
cd ~/project/bayes_course/EvSemMap/SemanticMap # 进入包含 src 的父目录
source devel/setup.bash    # 刷新环境变量

roslaunch evsemmap mapping.launch dataset:=deploy_rellisv3_4_1-30 method:=ebs result_name:=/home/xzy/deployTest/00004_optimized_ell_sf2   # 最后是一个文件名，不需要加 .map 后缀
# roslaunch evsemmap mapping.launch dataset:=deploy_rellisv3_4_1-30 method:=ebs result_name:=/home/xzy/datasets/Rellis-3D/results/00004_baseline/ ell:=0.3 sf2:=1.0
```

评估
```

roslaunch evsemmap map_reader.launch map_path1:=/home/xzy/deployTest/00004_optimized_ell_sf2.map
```